let str="JavaScript is used to perform actions";
let slice=str.slice(3,12);
console.log(slice);

//if negative value is given the word is taken from last
let slice1=str.slice(-8);
console.log(slice1);